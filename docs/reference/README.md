# Reference

> Status: grows as features land.

- **REST API / OpenAPI:** served at `/api/docs` (api-and-integrations epic).
- **Environment variables:** see [`.env.example`](../../.env.example).
- **Database schema:** generated from Drizzle (`libs/db`).
- **Scan profiles:** fields and defaults (recon-engine epic).
