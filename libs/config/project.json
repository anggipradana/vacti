{
  "name": "@vacti/config",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/config/src",
  "tags": ["lang:ts", "scope:lib"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p libs/config/tsconfig.json" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "eslint libs/config/src" }
    },
    "test:quick": {
      "executor": "nx:run-commands",
      "options": { "command": "vitest run --root libs/config" }
    }
  }
}
