---
name: Vault wiring (external keys) + central SSE
status: closed
created: 2026-06-03T19:09:19Z
updated: 2026-06-04T18:30:00Z
github: (will be set on sync)
depends_on: [001, 003, 004]
parallel: false
conflicts_with: []
---

# Task: Vault wiring (external keys) + central SSE

## Description

Sambungkan API Key Vault terenkripsi (OTX/LeakCheck/AI/webhook secrets) ke konsumen, dan
konsolidasikan endpoint **SSE** realtime (progress scan & refresh TI).

Menutup baris Checklist: 5.6, 6.5.

## Acceptance Criteria

- [ ] Service `getSecret(name, projectId)` mendekripsi dari vault saat dipakai; tak pernah ke log.
- [ ] Konsumen (OTX/LeakCheck/AI/webhook) menarik kunci via service ini.
- [ ] SSE terpusat: channel progress scan & refresh; auth + RBAC; reconnect aman.
- [ ] Integration test (vault round-trip via konsumen) + SSE smoke.

## Technical Details

- Reuse `api_keys` (AES-256-GCM) dari platform-foundation #007.

## Dependencies

- #001, #003, #004; platform-foundation #007.

## Effort Estimate

- Size: M
- Hours: 8

## Definition of Done

- [ ] Code implemented
- [ ] Vault + SSE teruji
- [ ] Code reviewed

## Status (2026-06-04)

Implemented: central SSE; AES-256-GCM vault primitives in `libs/auth/src/vault.ts`. **Remaining (gap
G6):** external provider keys (OTX / LeakCheck / AI) are still loaded from `.env` rather than the
encrypted vault, and there is no per-project key-management UI. Wire keys through the vault + UI.
Tracked as G6 (with PF/007).
