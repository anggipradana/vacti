---
name: Governance, Husky, commitlint & CI
status: open
created: 2026-06-03T19:09:19Z
updated: 2026-06-03T19:09:19Z
github: (will be set on sync)
depends_on: [001]
parallel: true
conflicts_with: [001]
---

# Task: Governance, Husky, commitlint & CI

## Description

Pasang "governance & conventions" gaya **ose-primer** + gerbang kualitas otomatis: hierarki
governance 6-lapis (`repo-governance/`), skeleton docs **Diátaxis** (`docs/{tutorials,how-to,
reference,explanation}`), Conventional Commits + commitlint, **Husky 3-stage** (pre-commit /
commit-msg / pre-push) dengan guard scripts, markdownlint, `.env.example` sebagai satu-satunya env
yang di-commit, dan **GitHub Actions** PR quality-gate (dynamic-detection + reusable) yang
menjalankan `nx affected` typecheck/lint/test + integration + e2e dengan service Postgres.

Menutup baris Feature Parity Checklist: **8.5 (guard), 8.7, 8.8, 8.9**.

## Acceptance Criteria

- [ ] `repo-governance/` berisi 6 lapis (vision, principles, conventions, development, agents,
  workflows) + `repository-governance-architecture.md`.
- [ ] `docs/` skeleton Diátaxis (tutorials/how-to/reference/explanation) + README index.
- [ ] `commitlint.config.js` (extends config-conventional); commit non-conventional ditolak.
- [ ] Husky: **pre-commit** = `git-identity-check.sh` + `check-no-env-staged.sh` + `lint-staged`;
  **commit-msg** = commitlint; **pre-push** = `nx affected -t typecheck lint test:quick` + `lint:md`.
- [ ] `check-no-env-staged.sh` menolak staging file `.env*` kecuali `.env.example`; `git-identity-
  check.sh` memverifikasi user.name/email ter-set.
- [ ] `.markdownlint-cli2.jsonc`, `.prettierrc.json` (printWidth 120), `.gitignore` (ignore `.env*`
  kecuali `.env.example`), `.env.example` ada.
- [ ] `.github/workflows/pr-quality-gate.yml` + reusable workflow(s) + `.github/actions/setup-node`;
  job: format(prettier), typescript gate (nx affected), integration (postgres service), e2e, markdown.
- [ ] CLAUDE.md, AGENTS.md, CONTRIBUTING.md, SECURITY.md, README.md ada & lulus markdownlint.

## Technical Details

- Disederhanakan dari ose-primer: **hanya TS** (buang matriks polyglot Go/JVM/dotnet/dll & rhino-cli).
- Trunk-based: branch pendek dari `main`, PR wajib lulus quality-gate.
- Postgres service container untuk job integration/e2e (`postgres:16`).
- Prettier `printWidth: 120`, `proseWrap: preserve`.

## Dependencies

- 001 (butuh `package.json`/Nx untuk wiring scripts & hooks). Berbagi `package.json` → conflicts_with 001.

## Effort Estimate
- Size: L
- Hours: 12

## Definition of Done
- [ ] Code implemented
- [ ] Hooks aktif (commit non-conventional & staging `.env` ditolak — diuji)
- [ ] CI workflow valid (actionlint) & hijau pada PR contoh
- [ ] Code reviewed
