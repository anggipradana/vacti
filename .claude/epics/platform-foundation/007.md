---
name: App shell, Project scoping & Settings/Vault UI
status: open
created: 2026-06-03T19:09:19Z
updated: 2026-06-03T19:09:19Z
github: (will be set on sync)
depends_on: [003, 004, 005]
parallel: false
conflicts_with: []
---

# Task: App shell, Project scoping & Settings/Vault UI

## Description

Bangun kerangka aplikasi `web`: layout root + tema dark/light, provider tRPC + React Query, error
boundary & toaster, inisialisasi `@vacti/ui` (shadcn/ui primitives + token desain). Tambah **scoping
multi-project** (`/[projectSlug]/...`) dan halaman **Settings** dasar: profil user, manajemen API
token, daftar user + assign role (SysAdmin), serta **API Key Vault** terenkripsi (AES-256-GCM).
Sediakan tRPC root router + Hono `/api` skeleton (health + auth probe) untuk diisi epic lain.

Menutup baris Feature Parity Checklist: **4.1, 5.6 (vault terenkripsi), 6.3/6.4 (UI), 7.6 (dark mode), 8.5**.

## Acceptance Criteria

- [ ] App shell: layout root, `next-themes` dark/light, provider tRPC+React Query, error boundary,
      toaster; `@vacti/ui` ter-init (button/input/dialog/table/dropdown/toast/form/card) + token desain
      (warna severity & risk, radius, typografi).
- [ ] Routing `/[projectSlug]/...`: buat/pilih project, guard `requireProjectAccess`, project switcher.
- [ ] Settings: profil user; buat/cabut API token (token tampil sekali); daftar user + assign role
      (khusus SysAdmin); **API Key Vault** simpan terenkripsi, tampil masked, dekripsi hanya saat dipakai.
- [ ] Enkripsi vault penuh: AES-256-GCM dgn `ENCRYPTION_KEY`; plaintext tak pernah ke log/response.
- [ ] tRPC root router + context (user/project/permission); Hono `/api` skeleton (health + whoami).
- [ ] Kontrol UI dihormati RBAC (`can(perm)`), penegakan tetap server-side.
- [ ] Integration/component test untuk vault (round-trip encrypt→store→decrypt) + e2e-able flow.

## Technical Details

- shadcn/ui + Radix; WCAG AA (kontras, keyboard nav, label SR).
- Vault: kolom ciphertext di `api_keys` (dari task 003); enkripsi di server action/tRPC mutation.
- Hono di-mount di route handler Next (`app/api/[[...route]]/route.ts`) atau app worker — pilih satu,
  dokumentasikan; endpoint nyata diisi epic api-and-integrations.

## Dependencies

- 003 (skema), 004 (session/token), 005 (RBAC guard).

## Effort Estimate

- Size: L
- Hours: 14

## Definition of Done

- [ ] Code implemented
- [ ] Vault round-trip terenkripsi teruji
- [ ] Flow login→project→settings dapat di-e2e
- [ ] Code reviewed

## Status (2026-06-04)

Implemented: app shell, project scoping, settings pages (`/settings/tokens`, `/settings/integrations`,
`/settings/reports`). **Remaining (gap G6):** no per-project **API-key vault UI** — OTX / LeakCheck /
AI keys are still read from `.env`, not the encrypted `apiKeys` table. Add a vault settings UI
(set/rotate/masked) and read keys from the vault. Tracked as G6.
