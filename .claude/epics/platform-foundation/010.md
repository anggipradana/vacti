---
name: Audit log writes & viewer
status: open
created: 2026-06-04T16:00:00Z
updated: 2026-06-04T16:00:00Z
github: (will be set on sync)
depends_on: [003, 005]
parallel: true
conflicts_with: []
---

# Task: Audit log writes & viewer

## Description

Aktifkan tabel `auditLog` (sudah ada di skema task 003) — saat ini **dorman** (tidak pernah ditulis,
tak ada viewer). Tulis entri audit untuk aksi penting (login, buat/hapus token, ubah status finding,
mulai/cancel scan, ubah report settings, ubah webhook/AI settings) dan sediakan halaman viewer
read-only per-proyek.

Menutup gap **G10** (lihat [02-FEATURE-PARITY-CHECKLIST.md](../../../docs/planning/02-FEATURE-PARITY-CHECKLIST.md)).

## Acceptance Criteria

- [ ] Helper `recordAudit({ actorId, action, resource, projectId, metadata })` dipanggil di server
      actions + API untuk aksi mutasi penting.
- [ ] Halaman `/settings/audit` (atau di Settings) menampilkan log terfilter per-proyek, terbaru dulu.
- [ ] Tidak menyimpan data sensitif (no secrets/plaintext) di `metadata`.
- [ ] Unit test helper + integration test (aksi → entri audit tertulis).

## Technical Details

- Tabel `auditLog` (actorId, action, resource, projectId, metadata jsonb, createdAt) sudah ada.
- Gunakan RBAC: hanya SysAdmin/Auditor yang bisa lihat audit (selaras task 005 / G1).

## Dependencies

- 003 (skema), 005 (RBAC enforcement untuk akses viewer).

## Effort Estimate

- Size: S
- Hours: 6

## Definition of Done

- [ ] Code implemented
- [ ] Unit + integration test hijau
- [ ] Viewer terlihat di UI
- [ ] Code reviewed
