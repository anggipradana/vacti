---
name: DB schema, Drizzle & config
status: closed
created: 2026-06-03T19:09:19Z
updated: 2026-06-04T16:00:00Z
github: (will be set on sync)
depends_on: [001]
parallel: true
conflicts_with: []
---

# Task: DB schema, Drizzle & config

## Description

Bangun lapisan data fondasi di `@vacti/db` dengan **Drizzle ORM** + drizzle-kit (sumber kebahasan
skema → tipe mengalir DB→Zod→tRPC→UI) dan loader env tervalidasi Zod di `@vacti/config`. Skema awal
mencakup entitas fondasi lintas-epic; migrasi pertama + seed admin & profil scan default placeholder.

Menutup baris Feature Parity Checklist: **8.3, 8.5 (env), 8.10 (seed kerangka), 4.1 (tabel projects)**.

## Acceptance Criteria

- [ ] `@vacti/db`: Drizzle client (pg) + skema: `users`, `sessions`, `api_tokens`, `projects`,
      `project_members`, `api_keys` (vault terenkripsi), `audit_log`. (Tabel domain lain milik epik lain.)
- [ ] drizzle-kit terkonfigurasi: `generate` + `migrate`; migrasi awal ter-commit di `drizzle/`.
- [ ] Seed idempoten: admin pertama (dari env), 1 profil scan default placeholder.
- [ ] `@vacti/config`: loader env Zod (`DATABASE_URL`, `ENCRYPTION_KEY`, `SESSION_SECRET`, `PORT`,
      `NODE_ENV`, opsional `OTX_API_KEY`/`LEAKCHECK_API_KEY`/AI keys) — **fail-fast** saat boot.
- [ ] Tipe row Drizzle di-reexport + helper Zod insert/select untuk dipakai lib lain.
- [ ] Integration test (service Postgres): migrate → seed → query users/projects.

## Technical Details

- PostgreSQL 16. Schema `public` untuk app; schema `pgboss` disiapkan terpisah (diisi task 006).
- `api_keys.value` disimpan ciphertext (AES-256-GCM) — kolom + helper enkripsi placeholder di sini,
  implementasi enkripsi penuh diselesaikan di 007 (Vault UI) memakai `ENCRYPTION_KEY`.
- Penamaan kolom snake_case; timestamps `created_at`/`updated_at` default now().
- `audit_log`: actor, action, resource, project_id, metadata jsonb, created_at.

## Dependencies

- 001 (workspace + lib `@vacti/db`/`@vacti/config`).

## Effort Estimate

- Size: M
- Hours: 10

## Definition of Done

- [ ] Code implemented
- [ ] Migrasi + seed jalan di Postgres bersih
- [ ] Integration test hijau
- [ ] Code reviewed
