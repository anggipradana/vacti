---
name: Adapters subfinder + httpx + WordPress detection
status: open
created: 2026-06-03T19:09:19Z
updated: 2026-06-03T19:09:19Z
github: (will be set on sync)
depends_on: [002]
parallel: true
conflicts_with: []
---

# Task: Adapters subfinder + httpx + WordPress detection

## Description

Adapter tahap enumerasi & probing: subfinder (skippable bila predefined subs) dan httpx (alive,
status, title, tech, webserver, CDN/CNAME). Tetapkan **penanda WordPress** berlapis.

Menutup baris Checklist: 1.1, 1.3, 1.8.

## Acceptance Criteria

- [ ] Adapter subfinder: argumen dari profil; **dilewati** bila target punya predefined_subdomains.
- [ ] Adapter httpx: parse JSONL → probe info + tech fingerprint; simpan `endpoints`/`technologies`.
- [ ] WordPress detect: tech fingerprint httpx → pola URL (`wp-content`/`wp-includes`/`wp-login.php`)
      → hormati tanda manual user; set `technologies.is_wordpress` + sumber.
- [ ] Unit test parser (fixture output subfinder/httpx), termasuk kasus skip & WP-detect.

## Technical Details

- Gunakan tool runner (#002). Profil menentukan rate/threads/timeout.
- Output mentah → file; data ter-normalisasi → model (#001).

## Dependencies

- #002 (runner), #001 (model).

## Effort Estimate

- Size: M
- Hours: 8

## Definition of Done

- [ ] Code implemented
- [ ] Parser + WP-detect teruji
- [ ] Code reviewed
