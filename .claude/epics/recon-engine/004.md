---
name: Adapters naabu + nuclei + conditional wordfence
status: closed
created: 2026-06-03T19:09:19Z
updated: 2026-06-04T16:00:00Z
github: (will be set on sync)
depends_on: [002]
parallel: true
conflicts_with: []
---

# Task: Adapters naabu + nuclei + conditional wordfence

## Description

Adapter tahap port scan & vuln scan: naabu (exclude-cdn) dan nuclei (per-severity, templates).
Tahap **nuclei + template WordPress/wordfence** dijalankan KONDISIONAL hanya untuk host bertanda
WordPress. Semua murni Go — tanpa Ruby.

Menutup baris Checklist: 1.4, 1.6, 1.7.

## Acceptance Criteria

- [ ] Adapter naabu: port scan cepat, exclude-cdn; simpan `ip_addresses`/`ports`.
- [ ] Adapter nuclei: jalankan per-severity sesuai profil; parse JSONL → `vulnerabilities`
      (name/severity/cve/cwe/cvss/request/response/matched_at).
- [ ] Wordfence: subset template WordPress/wordfence dijalankan **hanya** untuk host `is_wordpress`.
- [ ] Scope check benar untuk target level-subdomain (hindari out-of-scope).
- [ ] Unit test parser (fixture naabu/nuclei) + test gating wordfence (host non-WP tidak dipindai).

## Technical Details

- Gunakan tool runner (#002) + nuclei-templates dari image worker.
- Severity mapping ke enum -1..4 konsisten dgn model & risk-score.

## Dependencies

- #002 (runner), #001 (model). Sinyal WordPress dari #003.

## Effort Estimate

- Size: M
- Hours: 10

## Definition of Done

- [ ] Code implemented
- [ ] Parser + gating wordfence teruji
- [ ] Code reviewed
