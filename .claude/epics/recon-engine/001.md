---
name: Recon data models
status: open
created: 2026-06-03T19:09:19Z
updated: 2026-06-03T19:09:19Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Recon data models

## Description
Perluas `@vacti/db` dengan skema recon/VA: kontainer scan, hasil per-tahap, audit perintah, dan
timeline aktivitas. Sumber kebenaran tipe untuk seluruh epic recon.

Menutup baris Checklist: 1.18, 1.19, 4.2, 4.3, 4.4.

## Acceptance Criteria
- [ ] Tabel: `targets` (domain/org, predefined_subdomains[], custom_headers jsonb), `scans`
  (status, profile_id, started/finished, counts), `subscans`, `scan_activity` (tahap, status, ts),
  `commands` (tool, argv, exit_code, duration, output_path).
- [ ] Hasil: `subdomains`, `endpoints`, `ip_addresses`, `ports`, `technologies` (penanda WordPress),
  `vulnerabilities` (severity -1..4, name, cve[], cwe[], cvss, request, response, matched_at).
- [ ] `interesting_keywords`, `recon_notes` (per target/project).
- [ ] Relasi FK ke `projects` (scoping) + index untuk query tabel besar.
- [ ] Migrasi + helper Zod insert/select diekspor; integration test (migrate→insert→query).

## Technical Details
- Snake_case, timestamps default now(). Output tool mentah disimpan ke file (path di `commands`).
- `technologies.is_wordpress` boolean + sumber penanda (httpx/url/manual).

## Dependencies
- platform-foundation #003 (Drizzle/config) — eksternal.

## Effort Estimate
- Size: M
- Hours: 8

## Definition of Done
- [ ] Code implemented
- [ ] Migrasi + integration test hijau
- [ ] Code reviewed
