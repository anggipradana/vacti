---
name: Server-side DataTable + recon result views
status: open
created: 2026-06-03T19:09:19Z
updated: 2026-06-03T19:09:19Z
github: (will be set on sync)
depends_on: [001]
parallel: false
conflicts_with: []
---

# Task: Server-side DataTable + recon result views

## Description

DataTable generik server-side (filter/sort/paginate) + halaman hasil recon: subdomains, endpoints,
ports, technologies, vulnerabilities (detail dgn request/response, CVE/CWE/CVSS).

Menutup baris Checklist: 6.6, 7.x (hasil).

## Acceptance Criteria

- [ ] DataTable: kolom konfigurabel, filter/sort/paginate server-side via tRPC, URL state, loading.
- [ ] View: subdomains/endpoints/ports/technologies/vulnerabilities + halaman detail vuln.
- [ ] Badge severity & penanda WordPress; empty/error states.
- [ ] Component/integration test (filter/sort/paginate) dgn data fixture.

## Technical Details

- Dialiri API recon-engine (#007). Operator filter disederhanakan dari reNgine.

## Dependencies

- #001; recon-engine #007 (API).

## Effort Estimate

- Size: L
- Hours: 12

## Definition of Done

- [ ] Code implemented
- [ ] Tabel + view teruji
- [ ] Code reviewed

## Status (2026-06-04)

Implemented: recon result views/tables (client-side render). **Remaining (gap G11):** server-side
DataTable — API endpoints return full result sets with no `limit`/`offset`/`sort`/`filter` params.
Add server-side pagination/sort/filter + wire the table component. Tracked as G11.
